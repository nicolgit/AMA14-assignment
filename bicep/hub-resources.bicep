// hub-resources.bicep — hub lab resources have been removed.
// The hub VNet, VM, Bastion and NAT Gateway previously defined here are no
// longer part of the architecture. Network access to the spoke is now provided
// by the P2S VPN Gateway deployed in deploy-spoke-network.bicep.
