fastapi>=0.115,<1.0
uvicorn[standard]>=0.30,<1.0
sqlalchemy>=2.0,<3.0
# Prebuilt binary wheels everywhere except Windows ARM64 (no wheels published);
# there the pure-Python psycopg is used and relies on a local libpq client.
psycopg[binary]>=3.1,<4.0 ; platform_machine != "ARM64"
psycopg>=3.1,<4.0 ; platform_machine == "ARM64"
# Entra auth is a production-only path (Linux container). Skipped on Windows
# ARM64 dev boxes where azure-identity's native cryptography dep has no wheel;
# local development uses the explicit DATABASE_URL connection string instead.
azure-identity>=1.17,<2.0 ; platform_machine != "ARM64"
